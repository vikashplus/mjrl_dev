hydra:
    run:
        dir: ./outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
    sweep:
        dir: ./outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
        subdir: ${hydra.job.num}_${hydra.job.override_dirname}