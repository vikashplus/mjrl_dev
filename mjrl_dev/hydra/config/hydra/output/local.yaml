# @package _global_
hydra:
  run:
    dir: ./outputs/coss/mjrl/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
    subdir: ${seed}
  sweep:
    dir: ./outputs/coss/mjrl/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
    subdir: ${seed}