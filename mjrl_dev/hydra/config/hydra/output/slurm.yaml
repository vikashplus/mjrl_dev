# @package _global_
hydra:
  run:
    dir: /checkpoint/${env:USER}/outputs/coss/mjrl/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
    subdir: ${seed}
  sweep:
    dir: /checkpoint/${env:USER}/outputs/coss/mjrl/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
    subdir: ${seed}